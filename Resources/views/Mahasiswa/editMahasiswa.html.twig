{% block content %}
    <h1>Edit Mahasiswa Form</h1>
    {%  if (form is not null) %}
    <form action="{{ url('api_1_post_update_mahasiswa', {'id': mahasiswa.id}) }}" {{ form_enctype(form) }} method="POST">
        {{ form_widget(form) }}
        <input type="submit" value="submit">
    </form>
    {% endif %}
{% endblock %}
